version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: entropy_guard_db
      POSTGRES_USER: entropy_guard_user
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
